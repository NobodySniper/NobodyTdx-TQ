"""
config - 小白可配置参数（修改这里即可，不用动其他文件）

所有常量都被 backtest.py 直接 import 使用。
"""
import os

# ============================================================
# 股票（只填 6 位数字，自动补 .SH / .SZ / .BJ 后缀）
# ============================================================
STOCK_CODE = '603955'
STOCK_NAME = ''                            # 留空自动查询

# ============================================================
# 回测区间
# ============================================================
TARGET_START = '20230101'
TARGET_END = ''

# ============================================================
# 策略
# ============================================================
STRATEGY_NAME = 'MA金叉死叉'               # 策略显示名（报告用）
STRATEGY_KEY = 'ma_cross'

# 策略参数（通用字典，前端表单自动写入，backtest 自动读取）
# key 对应策略 params 的参数名，不需要为每个策略单独定义常量
STRATEGY_PARAMS = {
    'fast_period': 10,
    'slow_period': 20,
}

# ============================================================
# 资金与费率（A 股规则）
# ============================================================
INIT_CASH = 1000000
COMMISSION_RATE = 0.0001
STAMP_TAX_RATE = 0.001
SIZE_GRANULARITY = 100                     # A 股最小 100 股
SLIPPAGE = 0.0                             # 滑点（默认 0）

# ============================================================
# 路径
# ============================================================
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
LOG_DIR = os.path.join(BASE_DIR, 'logs')
REPORTS_DIR = os.path.join(BASE_DIR, 'reports')

# 本次启动 ID（用于子目录命名）
from datetime import datetime as _dt
RUN_ID = _dt.now().strftime('%Y%m%d_%H%M%S')
